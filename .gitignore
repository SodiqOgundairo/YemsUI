# Dependencies
node_modules/
playground/


> Note: This requires a different package name on NPM, not a scope alias.3. Publish normally: `npm publish`2. Remove `publishConfig` section or change to `"access": "public"`1. Change `name` in `package.json` to `"yems-ui"` (remove scope)If you want users to do `npm install yems-ui` instead:## Alternative: For future "yems-ui" package name---```npm logout# Logoutnpm info @yems-ui/core# Check published packagenpm publish --access=public# Publishnpm pack# Pack to tarballnpm publish --dry-run# Test locallynpm run build# Build```bash## Commands Cheatsheet---- Check `.npmignore` isn't excluding them- Ensure `files` array in `package.json` includes necessary files### "Missing files after install"- Run `npm pack` to see actual size- Check `.npmignore` is properly excluding dev files### "Files too large"- Check 2FA is working- Re-login: `npm logout` then `npm login`### "Unauthorized"- Or use a different scope (e.g., `@sodiqogundairo/yemsui`)- Choose a different name### "Package name already exists"## Troubleshooting---5. Description: Copy from `CHANGELOG.md`4. Title: `Release 1.0.0`3. Tag: `v1.0.0`2. Click "Create a new release"1. Go to [GitHub Releases](https://github.com/SodiqOgundairo/YemsUI/releases)### Create GitHub Release```[![npm version](https://badge.fury.io/js/%40yems-ui%2Fcore.svg)](https://badge.fury.io/js/%40yems-ui%2Fcore)```markdownUpdate README.md:### Add NPM Badge to README## Step 10: Post-Publication Setup---```git tag -l```bashView tags:```git push origin v1.0.0git tag -a v1.0.0 -m "Release version 1.0.0"```bash## Step 9: Create Git Tags (Optional but Recommended)---```npm install @yems-ui/core# Install in another project# https://www.npmjs.com/package/@yems-ui/core# View on webnpm info @yems-ui/core# Check npm registry```bashAfter publishing, verify the package:## Step 8: Verify Publication---```npm publish```bash### Future ReleasesThe `--access=public` flag makes the scoped package publicly available (required for org packages).```npm publish --access=public```bash### First Time Publishing## Step 7: Publish to NPM---- Commits the change- Creates a git tag- Updates `package.json`This automatically:```npm version major   # 1.0.0 → 2.0.0 (breaking changes)npm version minor   # 1.0.0 → 1.1.0 (new features)npm version patch   # 1.0.0 → 1.0.1 (bug fixes)```bashFor future releases, bump version using:Current version: `1.0.0`## Step 6: Version Management---```package/package.jsonpackage/LICENSEpackage/README.mdpackage/dist/index.csspackage/dist/index.d.tspackage/dist/index.mjspackage/dist/index.jspackage/dist/```Expected structure:```tar -tzf yems-ui-core-1.0.0.tgz | head -20```bashThis creates a `.tgz` file locally. Extract and inspect:```npm pack```bash## Step 5: Verify Package Contents---- [ ] No unexpected files included- [ ] Files sizes look reasonable- [ ] Version is correct- [ ] Only necessary files are included (check against `.npmignore`)This shows what would be published WITHOUT actually publishing. Check:```npm publish --dry-run```bash### Test the build (Dry Run)- `index.css` (Styles)- `index.d.ts` (TypeScript declarations)- `index.mjs` (ES Module)- `index.js` (CommonJS)Check that `dist/` folder is created with:```npm run build```bash### Build the library## Step 4: Build and Test Locally---```npm whoami```bashVerify login:- OTP (if 2FA is enabled)- Email- Password- UsernameYou'll be prompted for:```npm login```bash## Step 3: Login to NPM from Terminal---4. Add yourself as a member3. Choose "Free" plan2. Create organization named `yems-ui`1. Go to [https://www.npmjs.com/org/create](https://www.npmjs.com/org/create)For the scoped package `@yems-ui/core`:## Step 2: Create NPM Organization (Optional but Recommended)---   - Enable 2FA with an authenticator app (recommended) or SMS   - Go to Account Settings → Security4. **Enable 2FA** (Two-Factor Authentication) for security:3. Verify your email address2. Create an account with your email1. Go to [https://www.npmjs.com/signup](https://www.npmjs.com/signup)## Step 1: Setup NPM Account---This guide walks you through everything needed to publish `@yems-ui/core` to NPM.
# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# Environment
.env
.env.local
.env.*.local

# OS
.DS_Store
Thumbs.db

# Build/Development
.turbo/
.turbo
dist/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Project specific
.github/instructions/

# Note: dist/ is tracked in npm but not in git
# This allows npm publish to build fresh distributions
